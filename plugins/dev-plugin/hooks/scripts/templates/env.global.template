# dev-plugin Global Environment Variables
# Location: ~/.claude/plugins/dev-plugin/.env
#
# ═══════════════════════════════════════════════════════════════
# IMPORTANT: This is your GLOBAL .env file
# ═══════════════════════════════════════════════════════════════
#
# These environment variables are available to ALL projects.
# To override for specific projects, create .claude/.env
#
# Environment Priority (later overrides earlier):
#   1. ~/.claude/plugins/dev-plugin/.env (this file - global)
#   2. .claude/.env (project-specific overrides)
#   3. System environment variables
#
# Security:
#   ✅ NEVER commit this file to git!
#   ✅ Keep your API keys and secrets here
#   ✅ This file persists across plugin updates
#
# Storage Location:
#   ✅ This file: ~/.claude/plugins/dev-plugin/ (stable, persists)
#   ❌ NOT in:   ~/.claude/plugins/cache/dev-plugin/ (ephemeral, wiped)
#

# ═══════════════════════════════════════════════════════════════
# LANGFUSE CONFIGURATION
# ═══════════════════════════════════════════════════════════════
#
# Get these credentials from your Langfuse instance:
#   1. Visit http://localhost:3000 (or your Langfuse URL)
#   2. Go to: Settings → API Keys
#   3. Generate new keys if needed
#

# Langfuse API Keys
LANGFUSE_PUBLIC_KEY=pk-lf-CHANGE-ME
LANGFUSE_SECRET_KEY=sk-lf-CHANGE-ME

# Langfuse Host URL
LANGFUSE_HOST=http://localhost:3000

# ═══════════════════════════════════════════════════════════════
# OPTIONAL: ADDITIONAL ENVIRONMENT VARIABLES
# ═══════════════════════════════════════════════════════════════

# NOTE: Langfuse compose_path is NOT needed in global config
# The setup script always uses ~/.langfuse/docker-compose.yml
#
# If you need a different Langfuse location for specific projects,
# set compose_path directly in .claude/dev-plugin.yaml (not here):
#
#   observability:
#     langfuse:
#       compose_path: /path/to/project/langfuse/docker-compose.yml

# ═══════════════════════════════════════════════════════════════
# PROJECT-SPECIFIC OVERRIDES
# ═══════════════════════════════════════════════════════════════
#
# To override environment variables for a specific project:
#   Create: .claude/.env
#
# Example project .env override (.claude/.env):
#
#   # Use different Langfuse instance for this project
#   LANGFUSE_PUBLIC_KEY=pk-lf-acme-client-key
#   LANGFUSE_SECRET_KEY=sk-lf-acme-client-secret
#   LANGFUSE_HOST=https://langfuse.acme-corp.com
#
# Project .env values override global .env values.
#
