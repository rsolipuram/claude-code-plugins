# dev-plugin Environment Variables
# Copy this file to .claude/.env in your project
# or to ~/.claude/plugins/dev-plugin/.env for global settings
#
# IMPORTANT: Never commit .env files to git!
# The .gitignore should exclude .env files automatically.

# ===================================
# LANGFUSE CREDENTIALS
# ===================================
# Get these from your Langfuse instance:
# 1. Start Langfuse: docker compose -f docker-compose.langfuse.yml up -d
# 2. Open http://localhost:3000
# 3. Go to Settings → API Keys
# 4. Create a new API key pair
# 5. Copy the values here

LANGFUSE_PUBLIC_KEY=pk-lf-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
LANGFUSE_SECRET_KEY=sk-lf-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
LANGFUSE_HOST=http://localhost:3000

# ===================================
# OPTIONAL SETTINGS
# ===================================
# Override defaults if needed

# USER=your-username               # Default: uses $USER from system
# LANGFUSE_VERSION=1.0.0           # Plugin version for tracking
